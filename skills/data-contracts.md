# Data Contracts

- `ResearchDataset` is the canonical phase-1 merged historical dataset
- Every dataset report stores source provenance assumptions and degraded fields
- Trade reasoning is exported into file artifacts, not persisted to Mongo in the validated phase-1 path
- Regime states, signals, trades, and daily states are exported as JSON artifacts per run
