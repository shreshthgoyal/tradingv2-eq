# Market Curator Rules

- Use `nselib` first for NSE market data
- Use `screener-scraper-pro` for fundamentals and shareholding
- Fall back only for missing data surfaces such as FII/DII activity
- Normalize all snapshots to one stable internal structure
